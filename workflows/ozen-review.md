---
description: 對當前變更進行程式碼審查
---

# /ozen review

// turbo-all

1. **載入角色思維**
   - 讀 `roles/code-reviewer.md` — 嚴格的正確性與架構審查

2. **載入技能**
   - 讀當前修改最接近的 skill（例如 UI → `visual-design-foundations`，測試 → `javascript-testing-patterns`）

3. **執行 code-review workflow**
   - 按 `workflows/code-review.md` 的步驟執行

4. **紀錄（Recorder）**
   - 審查結果寫入 `spec/CHANGELOG.md`
